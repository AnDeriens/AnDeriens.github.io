version: 2.1
orbs:
  node: circleci/node@3.0.0
working_directory: ~/
jobs:
  build:
    docker: 
      - image: 'cimg/base:stable'
    steps:
      - checkout
      - node/install:
          install-yarn: true
      - run: node --version
workflows:
  node-tests:
    jobs:
      - node/test
